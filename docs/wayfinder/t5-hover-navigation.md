# T5 Hover accordion + real navigation

**Blocked by:** T1

acceptsMouseMovedEvents; onHover 160ms accordion; jump(to:) per adapter (codex:// deep link, terminal detection for Claude Code); collapse after jump; shake on missing app. Rubric #7 #8 #9.

## Evidence

_(pending)_
